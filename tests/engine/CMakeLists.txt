add_executable(grapple_engine_test
  engine_test.cpp
  scene_test.cpp
  graphics_test.cpp
  camera_test.cpp
  actor_test.cpp
  render_test.cpp
  input_test.cpp
  physics_test.cpp
  assets_test.cpp
  light_test.cpp
  platform_test.cpp
)
target_link_libraries(grapple_engine_test
  PRIVATE
    Grapple::Engine
    Grapple::Image
    GTest::gtest_main
)
cppboot_set_project_warnings(grapple_engine_test)
gtest_discover_tests(grapple_engine_test DISCOVERY_TIMEOUT 60)

find_package(Python3 COMPONENTS Interpreter)
if(Python3_Interpreter_FOUND)
  add_test(NAME engine_link_audit
    COMMAND ${Python3_EXECUTABLE} ${CMAKE_SOURCE_DIR}/scripts/link_audit.py
            $<TARGET_FILE:grapple_engine_test>
  )
endif()
