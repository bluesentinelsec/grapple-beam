# Unit tests — one subdirectory per component under test.
# Browser builds run the wasm test page instead of the native CTest suites.
if(EMSCRIPTEN)
  add_subdirectory(web)
  return()
endif()

add_subdirectory(version)
if(GRAPPLE_BUILD_IMAGE)
  add_subdirectory(image)
endif()
if(GRAPPLE_BUILD_TTF)
  add_subdirectory(ttf)
endif()
if(GRAPPLE_BUILD_MIXER)
  add_subdirectory(mixer)
endif()
if(GRAPPLE_BUILD_NET)
  add_subdirectory(net)
endif()
if(GRAPPLE_BUILD_GFX)
  add_subdirectory(gfx)
endif()
if(GRAPPLE_BUILD_EXTRAS)
  add_subdirectory(extras)
endif()
if(GRAPPLE_BUILD_FORMATS)
  add_subdirectory(formats)
endif()
if(GRAPPLE_BUILD_VFS)
  add_subdirectory(vfs)
endif()
if(GRAPPLE_BUILD_PHYSICS)
  add_subdirectory(physics)
endif()
if(GRAPPLE_BUILD_GUI)
  add_subdirectory(gui)
endif()
if(GRAPPLE_BUILD_REGEX)
  add_subdirectory(regex)
endif()
if(GRAPPLE_BUILD_LIGHT)
  add_subdirectory(light)
endif()
if(GRAPPLE_BUILD_ENGINE)
  add_subdirectory(engine)
endif()
if(GRAPPLE_BUILD_LUA AND GRAPPLE_BUILD_RUBY)
  add_subdirectory(script)
endif()
if(GRAPPLE_BUILD_HTTP)
  add_subdirectory(http)
endif()
if(GRAPPLE_BUILD_CPP)
  add_subdirectory(cpp)
endif()
if(GRAPPLE_BUILD_TILED AND GRAPPLE_BUILD_CPP AND GRAPPLE_BUILD_LUA AND GRAPPLE_BUILD_RUBY)
  add_subdirectory(tiled)
  add_subdirectory(genbindings)
endif()

# The installed SDK and the shipped runner, checked as a consumer sees them.
add_subdirectory(sdk)
