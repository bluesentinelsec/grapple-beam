add_executable(grapple_light_test
  light_test.cpp
)
target_link_libraries(grapple_light_test
  PRIVATE
    Grapple::Light
    GTest::gtest_main
)
cppboot_set_project_warnings(grapple_light_test)
gtest_discover_tests(grapple_light_test DISCOVERY_TIMEOUT 60)

find_package(Python3 COMPONENTS Interpreter)
if(Python3_Interpreter_FOUND)
  add_test(NAME light_link_audit
    COMMAND ${Python3_EXECUTABLE} ${CMAKE_SOURCE_DIR}/scripts/link_audit.py
            $<TARGET_FILE:grapple_light_test>
  )
endif()

# The embedded shaders are generated from light/shaders/* and
# engine/shaders/*; one --check covers both bundles. Same guard
# bindgen_freshness gives the generated bindings.
if(Python3_Interpreter_FOUND)
  add_test(NAME embedded_shaders_freshness
    COMMAND ${Python3_EXECUTABLE} ${CMAKE_SOURCE_DIR}/scripts/embed_shaders.py --check
  )
endif()
