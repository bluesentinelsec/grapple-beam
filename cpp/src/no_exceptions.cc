// no_exceptions.cc — compiles every bindings header with -fno-exceptions,
// making "no exceptions, traceable control flow" a build-enforced property
// rather than a convention. Original Grapple code (zlib).
#include "grapple/grapple.h"
